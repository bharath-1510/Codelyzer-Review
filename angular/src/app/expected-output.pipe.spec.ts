import { ExpectedOutputPipe } from './expected-output.pipe';

describe('ExpectedOutputPipe', () => {
  it('create an instance', () => {
    const pipe = new ExpectedOutputPipe();
    expect(pipe).toBeTruthy();
  });
});
